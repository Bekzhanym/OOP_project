package edu.university.domain.value;

public enum Role {
    STUDENT,
    TEACHER,
    EMPLOYEE,
    ADMIN,
    MANAGER,
    /** Research-only employee (not Teacher / Student). */
    RESEARCH_STAFF
}
